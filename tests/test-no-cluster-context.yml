name: Test no cluster context
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          secret-name: my-test-secret
          secret-type: generic
      - name: Assert action failed gracefully without cluster context
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed as expected when no KUBECONFIG is set"
          else
            echo "FAIL: expected action to fail without cluster context, got outcome: $OUTCOME"
            exit 1
          fi
