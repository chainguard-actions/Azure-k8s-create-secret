name: Test generic secret with base64 data
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up mock kubeconfig
        run: |
          mkdir -p /tmp/kube
          cat > /tmp/kube/config <<'EOF'
          apiVersion: v1
          kind: Config
          clusters:
          - cluster:
              server: https://127.0.0.1:6443
              insecure-skip-tls-verify: true
            name: mock-cluster
          contexts:
          - context:
              cluster: mock-cluster
              user: mock-user
              namespace: default
            name: mock-context
          current-context: mock-context
          users:
          - name: mock-user
            user:
              token: mock-token
          EOF
          echo "KUBECONFIG=/tmp/kube/config" >> $GITHUB_ENV

      - id: action
        uses: .
        with:
          secret-name: my-data-secret
          secret-type: generic
          data: '{"key1":"dmFsdWUx"}'
          namespace: default
        continue-on-error: true

      - name: Assert action attempted to run with data input
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # With a mock kubeconfig set, the action should get past the
          # "Cluster context not set" check and attempt to connect to the API.
          # It will fail with a connection error (not a context error), which is expected.
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed as expected (mock cluster not reachable)"
          else
            echo "PASS: action succeeded"
          fi
          echo "PASS"
